# ice-cream-bot 作業メモ

このファイルは Claude Code / Cowork が最初に読む前提で書いている。
「どこまで終わっていて、次に何をすればよいか」だけを書く。仕様は README.md を見ること。

---

## いまの状態（2026-08-29 時点）

**v2 が本番で稼働中。ただし投稿は止めてある。**

- `main` = v2。Vercel 本番にデプロイ済み。10分おきの cron が正常動作
- `MAX_DAILY_POSTS=0` を設定しているため、**判定・照合まで実行してキューに溜めるだけ**。X には1件も投稿しない
- 旧 v1 は `backup/pre-renewal-20260828` ブランチに退避済み

### 解決済み（旧botが動かなかった原因）

**Upstash Redis のデータベースが消滅していた。** cron が24時間で8回とも
`getaddrinfo ENOTFOUND direct-lemur-82033.upstash.io` で500終了していた。
コードの問題ではなかった。新しい Upstash（`upstash-kv-charcoal-ladder`）を
作成して接続済み。

### 既知の落とし穴: フィードのキャッシュ（2026-08-31に対処済み）

PR TIMES のフィードが **最大12時間古い状態で返ってくる**ことがあった。
10分おきのスキャンで「新規0」が続き、ときどき100件超がまとめて出現する挙動になる。
配信側かCDNのキャッシュが原因。速報botとしては投稿の中身以前の問題になる。

対処として取得URLに毎回異なるクエリを付け、`Cache-Control: no-cache` を送っている。
**毎回のスキャンで `フィード最新: …（N分前）` をログに出しているので、
Nが90を超えていたら再発を疑うこと**（90分超はエラーとしても記録される）。

### 実機で検証済みの項目

| 項目 | 状態 |
|---|---|
| Upstash Redis 読み書き | ✅ |
| PR TIMES 全社フィード取得・重複排除 | ✅ 200件取得→新規5件を0.4秒 |
| Claude 判定・投稿文生成 | ✅ 実記事4本で確認 |
| 事実照合（自動投稿の可否） | ✅ 正しい記事は通し、捏造は弾く |
| X 認証 | ✅ @icemania |
| X 画像アップロード（v2 エンドポイント） | ✅ 実際に media_id 取得 |

X Premium でも **API 経由では280文字（重み）の制限が残る**ため、
`MAX_TWEET_WEIGHT` は 280 のまま（環境変数で変更可）。

## 次にやること

### ① 溜まった投稿待ちを確認する

https://ice-cream-bot.vercel.app/admin を開き（パスワードは `ADMIN_SECRET`）、
「投稿待ち」タブに実際のアイス記事が2〜3件溜まっているか見る。
文章と事実が問題なければ次へ。

### ② 投稿をONにする

**`/admin` の上部にあるスイッチで切り替える。Redeploy は不要。**
設定は Redis に保存され、次のスキャンから効く。

環境変数 `MAX_DAILY_POSTS` は「Redisに設定が無いときの既定値」に降格した。
運用中の切り替えのたびに Vercel の設定変更と Redeploy を要求するのは
運用として筋が悪いため。

ONにすると、溜まっていた投稿待ちから順に自動投稿が始まる。

### ③ 最初の数件を見る

`/admin` の「投稿済み」タブか、Vercel のログで
`[scan:live] 取得… 投稿1 …` の行を確認する。

## 実機で確認できていないこと

以下は 2026-08-29 時点で **まだ本番の自動投稿として動いていない**。
`MAX_DAILY_POSTS` を 12 にした直後の数件で必ず確認すること。
（画像アップロード・フィードのパース・照合の通過率は実機確認済み。上の表を参照）

1. **cron が拾って「自動で」投稿されるところまでの通し**
   2026-08-31 に `/admin` から**手動で承認して**Xへ投稿することには成功した。
   文面の生成・事実照合・画像付き投稿までは実記事で動いている。
   残るのは最後の一区間、「cron が拾う → 人の操作なしで投稿される」だけ。
   `MAX_DAILY_POSTS` を 12 にした直後の1件を、Vercel のログ
   （`[scan:live] … 投稿1 …`）と実際のタイムラインの両方で確認する。

2. **投稿の頻度が実運用で妥当か**
   `MAX_DAILY_POSTS=12` / `MIN_POST_GAP_MINUTES=5` は机上の値。
   アイスの新商品は1日数件なので上限に当たることはまず無い見込みだが、
   大型の発表が重なった日に連投しすぎないか、最初の1週間は様子を見る。

3. **承認待ちに回る割合**
   実記事4本での検証では、判定側の問題は解消し、残った警告は
   「税込と書いたが原文にない」「全国と書いたが一部店舗」など
   照合が正しく仕事をしたケースだった。
   ただし母数が4本なので、実運用で同じ警告が繰り返し出るようなら
   `docs/operations.md` の「よくある状況と判断」に従って調整する。

---

## 環境変数の扱い

**Vercel の「Sensitive」指定は使わないこと。**
Sensitive にした値は書き込み専用になり、ダッシュボードからも
`vercel env pull` からも二度と取得できない。
実際に `ADMIN_SECRET` を Sensitive にして値が分からなくなり、
管理画面に入れなくなった（作り直すしかなかった）。

`.env.local` を `vercel env pull` で更新すると、Sensitive 指定の項目は
`[SENSITIVE]` というプレースホルダで上書きされる。**手元の実値が消えるので、
pull の前に控えを取ること。**

管理画面は `ADMIN_SECRET` と `CRON_SECRET` のどちらでも開ける。
CRON_SECRET は /api/scan を叩けて投稿まで起こせるため、
権限としてはもともと ADMIN_SECRET より広く、後退にはならない。
締め出されないための保険。

---

## 自動投稿の対象（2026-09-01 追加）

事実照合を通っても、自動投稿するのは `src/lib/trust.ts` の
`AUTO_POST_PUBLISHERS` に載っている配信元だけ。それ以外は承認待ちに回る。

- 判定は PR TIMES の配信企業名（`dc:corp`）のみ。Claude が抽出した
  `maker` は使わない（抽出ミスがそのまま外に出るのを避けるため）。
- 大手が PR 代理店経由で配信した場合も承認待ちに回る。
  「確認が増える」側の外れ方なので許容する。
- 会社を足すのは `AUTO_POST_PUBLISHERS` に1行足すだけ。
  安易に足さないこと。ここが自動投稿の唯一の門になっている。

---

## あいぱく関連の収集（2026-09-01 追加）

`src/lib/filter.ts` の `WATCH_TERMS`（あいぱく／アイスクリーム万博／
日本アイスマニア協会）に当たった記事は、発売告知でなくても候補に通す。
イベントの開催告知や出展情報も見たいため、除外語・発売告知語の判定を
どちらも飛ばす。

- 拾ったものは **必ず承認待ち**。新商品告知として成立していても自動投稿しない。
- 新商品告知でない場合は Claude が投稿文を返さないので、
  見出しとURLだけを入れた項目を承認待ちに積む。文面は人が書く。

---

## 承認待ちの通知（Web Push・2026-09-01 追加）

承認待ち・投稿待ちが増えたとき、登録した端末へ通知を送る。
通知をタップすると /admin が開く。

必要な環境変数（`node scripts/gen-vapid.mjs` で作る。Sensitive は付けない）:

- `VAPID_PUBLIC_KEY` … ブラウザに配る鍵。秘密ではない
- `VAPID_PRIVATE_KEY` … 秘密鍵。絶対に共有しない
- `VAPID_SUBJECT` … `mailto:自分のアドレス`

仕組み:

- `public/sw.js` が通知を受けて表示する。`skipWaiting` で即時反映する
- 端末の購読は Redis の `v2:push:subs`（endpoint がフィールド名なので重複しない）
- 送信時に 404 / 410 が返った購読はその場で削除する。
  放置すると毎回失敗して「届かなくなった」と勘違いする
- 1回のスキャンにつき通知は1通だけ。通知の `tag` を固定しているので、
  10分おきの通知が積み上がらず置き換わる
- 通知の失敗はスキャンを止めない（投稿の可否とは無関係のため）

未設定でも動く: VAPID鍵が無ければ送信をスキップしてログに残すだけ。

---

## 触ってはいけないところ

- `src/lib/verify.ts` の照合を緩めて「全部自動投稿」にすること。
  これをやると誤情報がそのまま公開アカウントに出る。
- `src/lib/store.ts` に `redis.keys()` を足すこと。v1 のタイムアウト要因。
- 実行ロック（`acquireRunLock`）の削除。Vercel の cron は二重配信しうる。
- `.env.check` などをコミットすること。`vercel env pull` の出力は秘密情報を含む。
  `.gitignore` で `.env.*` を除外済み。

---

## この環境特有の注意

このフォルダは以前、**git がファイルを削除できずブランチ切替やコミットが
途中で壊れる**状態だった（`_to_delete/git-lock-cleanup/` や `.git/HEAD.lock` の
残骸がその痕跡）。ローカルと GitHub/Vercel の中身がズレる原因になっていた。
同じ症状が出たら `.git/HEAD.lock` を消して `git reset --hard <ブランチ>` で復旧する。
